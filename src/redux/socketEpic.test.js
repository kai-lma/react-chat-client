describe('Socket Epic Test', () => {
  it('should receive event from server and parse to action', () => {
    expect(1).toBe(1);
  });

  it('should send event to server from redux action', () => {
    expect(1).toBe(1);
  });

  it('should handle error not to stop the stream', () => {
    expect(1).toBe(1);
  });
});
